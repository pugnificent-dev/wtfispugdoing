{
  "name": "whatispugdoing",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
