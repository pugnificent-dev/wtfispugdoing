{
  "name": "whatispugdoing",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css script.js dist/ && cp -r assets dist/ && rm -f dist/assets/ref-mockup.png dist/assets/meagan-source.png dist/assets/_crop-test-*.jpg",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
